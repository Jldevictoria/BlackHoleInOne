using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class AstralBodyController : MonoBehaviour
{
    // public variables
    public float mass;
    // private variables

    // Start is called before the first frame update
    void Start()
    {
        //mass = 1.0f;   
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
